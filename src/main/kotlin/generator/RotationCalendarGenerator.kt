package generator

import model.Resident
import model.Rotation
import model.RotationCalendar

class RotationCalendarGenerator {

    fun generateCalendar(residents: List<Resident>, rotations: List<Rotation>, residentsToLeaveInCore: Int): RotationCalendar {





        return RotationCalendar(listOf())
    }

}